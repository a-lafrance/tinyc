main
var a, b, c, d, e, f;

{
    let a <- 1;
    let b <- 2;
    let c <- a;
    let d <- call InputNum();
    let e <- a + b + c + d;

    if e > d
    then
        if b > a
        then
            let f <- 34;
        else
            let f <- 35;
        fi
    else
        let f <- 420;
    fi;

    call OutputNum(f);
    call OutputNewLine();
}.

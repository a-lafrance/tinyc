main
var a, b, c, d, e;

{
    let a <- 1;
    let b <- 2;
    let c <- a;
    let d <- call InputNum();
    let e <- a + b + c + d;

    if e > d
    then
        call OutputNum(a);
    else
        call OutputNum(b);
    fi;

    call OutputNewLine();
}.

main
var a, b, c;

void function empty();
{

};

function add(x, y);
{
    return x + y;
};

{
    let a <- call InputNum();
    let b <- call InputNum();
    let c <- call add(a, b);

    call OutputNum(c);
    call OutputNewLine();

    call OutputNum(call add(a + 1, b + 1));
    call OutputNewLine();
}.

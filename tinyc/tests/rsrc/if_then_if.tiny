main
var a;

{
    let a <- call InputNum();

    if a >= 0
    then
        if a == 0
        then
            call OutputNum(0);
        else
            call OutputNum(1);
        fi;
    else
        call OutputNum(2);
    fi;

    call OutputNewLine();
}.
